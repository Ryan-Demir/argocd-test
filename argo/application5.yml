apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-5
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Ryan-Demir/argocd-test.git
    targetRevision: HEAD
    path: demo5
  destination:
    server: https://kubernetes.default.svc
    namespace: demo-5
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true